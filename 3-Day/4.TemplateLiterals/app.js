// template Literals ``
// template literals are used to create a string that is a template for a string that will be created
// at runtime. They are used to insert values into a string.

console.log(`This is some
  random text`);

console.log(`${2 + 2}`);

const firstName = "koushik";
const lastName = "roy";
console.log(`${firstName} ${lastName}`);
